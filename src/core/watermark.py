class Watermark:
    """Represents a watermark with its properties."""

    def __init__(self, text, position, transparency):
        self.text = text
        self.position = position
        self.transparency = transparency