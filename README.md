# Photo-Watermark-2

Local Windows app to add text/image watermarks to photos. See detailed PRD in `docs/PRD.md`.

Quick start (dev):

```bash
python -m venv .venv
.venv\\Scripts\\pip install -r requirements.txt
python -m src.app
```